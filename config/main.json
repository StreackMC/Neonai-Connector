{
  // 机器人名称
  "name": "澪奈",
  // 机器人次要名称（部分场景会使用）
  "subname": "Neonai",
  // 命令前缀，消息 trimStart 后匹配任一即触发命令解析
  "prefix": ["/", "!"],
  // 日志大小达到多少就压缩
  "maxLogFileSize": 1048576,
  // 日志是否要全量记录，为 false 时会自动压缩
  "detailedLog": true,
  // 控制恶意文本过滤器，默认使用 Qwen3Guard-4B 模型，若需修改请修改 AI.js 的源代码。
  "moderate": {
    // 启用与否
    "enabled": true,
    // 使用何种硬件加速，高级选项
    "accualre": "",
    // 模型镜像站
    "mirror": "https://hf-mirror.com/"
  }
}