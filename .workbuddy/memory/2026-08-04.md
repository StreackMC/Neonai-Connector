# 2026-08-04 工作日志

## 项目架构阅读

通读 Neonai 全部源码，梳理出组合根 + 依赖注入架构：

- `main.js`：入口（组合根），通过 `getConfigs()` / `getLogger()` 惰性单例组装子模块，含 PID 锁与优雅关闭
- `src/conf.js`：声明式配置，`CONFIG_PATHS` 映射内部名→路径，JSON5 解析
- `src/logger.js`：分模块日志，`LOG_TYPES` 声明式定义，Proxy 路由，console+文件双输出，gzip 轮转，console 劫持
- `src/QQBot/entry.js`：QQ 频道机器人监听（qq-guild-bot）
- `src/Handler/`：空目录，预留消息处理器

发现的问题：README.md 过时（仍提 websocket.js）、logs/ 残留空目录。

## 凭据管理澄清

用户说明 `config/secret.json` 是模板文件（key 为随意填充），已入库 Git；根目录 `secret.json` 包含真实凭据，gitignore 已正确排除。

## Git 约定确立

记录全局强制 Git 提交约定：用户名 NeoNai、邮箱 neonai+coding@kdxiaoyi.top（仅命令行参数），Conventional Commits 中英双语，只提交必要文件，记忆文件一并入库。

## 记忆文件初始化

创建 `.workbuddy/memory/` 目录，写入 MEMORY.md（长期项目笔记）和本日日志，并提交至 Git。

## 架构重构：三层分离

按用户要求重构项目为 SYSTEM / Handler / Platform 三层架构：

- **src/system/**（新增）— 底层基础设施
  - `entry.js`：组合根（bootstrap, 惰性单例 getConfigs/getLogger, 平台加载, CLI 启动）
  - `cli.js`：注册式命令系统 + 带引号转义的参数解析器 + readline REPL
  - `pid.js`：从 main.js 提取的 PID 进程锁（通过参数接收 logger）
  - `conf.js`、`logger.js`：从 src/ 根目录移入（git mv 保留历史）
- **src/platform/**（新增）— 平台交互适配
  - `qqbot.js`：从 src/QQBot/entry.js 移入，import 路径更新为 ../system/entry.js，新增 `qqbot status` 命令
- **src/Handler/** — 保留作为 IN→OUT 中转层
- **main.js** — 精简为瘦入口（仅调用 system/bootstrap 并兜底错误）

已清理残留空目录 logs/Chat、logs/WebSocket。README.md 和 jsconfig.json 同步更新。

## CLI 命令系统增强

- registerCommand 新增 options 参数：description / argsCount / usage
- 参数校验：自动检查参数数量（精确值或 [min, max] 范围），不匹配时红色高亮错误
- 错误提示防抖：800ms 内相同错误不重复输出
- TAB 补全：按 TAB 自动补全命令名，多候选项时列出
- help 输出美化：显示每条命令的描述和用法
