# 2026-08-08 工作日志

## 新增 handler 扩展：streackserver（命令 "mc"）

- 文件：`extensions/handler/streackserver/index.js`（按扩展自动加载约定放在子目录 index.js）
- 注册命令 `mc`：查询头部常量 `ADDRESS = "http://localhost:8080/api/status"`，根据返回数据格式化展示。
- 查询失败（网络错误/超时/接口非 2xx/显式 offline/空响应）一律返回精确文案 `× 服务器离线`。
- 数据格式化兼容真实接口结构：`version`/`motd` 为对象（优先取 `.name/.clean/.raw/.text`，否则 JSON）；`players` 嵌套对象取 online/max；`*_at` 时间戳字段转本地可读时间。
- 验证：语法检查通过；对真实 8080 服务执行 mc 输出正常；mock fetch 抛错验证失败路径精确返回 `× 服务器离线`。
- 命令无参数，`usage: 'mc'`，无权限限制。

## 记忆维护

- 更新 MEMORY.md：修正 `src/Handler`→`src/handler` 笔误；新增「扩展系统」章节（extensions 自动加载约定）；刷新「已知待办」。

## mc 命令输出格式微调（用户反馈）

- 丢弃 `expires_at` 冗余字段。
- `retrieved_at` 改名「更新时间」，时间戳改用中文式 `YYYY-MM-DD HH:mm:ss`（24h 本地时区），不用 `toLocaleString` 美式。
- `memory` 改中文「内存」：`已用 / 上限（占用 X%）`，自动 GB/MB。
- `tps` 加中文标签并展开 `实时/1m/5m/15m`，`live<0` 显示 N/A。
- 用户偏好：技术字段要中文标签、时间不用美式写法（值得在类似展示场景沿用）。
